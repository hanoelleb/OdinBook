<!DOCTYPE html>
<html>
    <head>
        <link rel='stylesheet' href='/stylesheets/style.css' />
    </head>
    <body>
	<%- include navbar %>
	<h2><%= title %></h2>
	
	<% if (user.avatar.data != null) { %>
	    <img src='/profile/photo'>
	    <a href='/profile/upload'>Change photo</a>
	<% } else { %>
            <a href='/profile/upload'>Add profile picture</a>
	<% } %>
	
	<p>Name: <%= user.name %></p>
	<p>Age: <%= user.age ? user.age : 'Add your age by editing your profile!'%></p>
	<p>Birthday: <%= user.birthday ? user.birthday : 'Add your birthday by editing your profile!'%></p>
	<p>Bio: <%= user.bio ? 
	    user.bio : 'Add a bio by editing your profile!'%><p>
	<a href='/profile/edit'>Edit your profile</a>
	<h3>Your posts</h3>
	<% for (var i = 0; i < posts.length; i++ ) { %>
	    <div class='post'>
		<a href='/'><%= user.name %></a><br>
		<%= posts[i].content %>
		<br>
		<span>likes: <%= posts[i].likes %>
                  date: <%= posts[i].date %></span>
            </div>
	<% } %>
    </body>
</html>
